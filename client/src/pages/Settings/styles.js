import { styled } from "@mui/material";

export const Paragraph = styled('p')(()=>({
    fontSize:'21px',
    fontWeight:'500',
    padding:0,
    margin:0
}))

export const SettingsItem = styled('div')(()=>({
    marginBottom:'10px'
}))